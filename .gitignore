node_modules/
node_modules/.vite/
dist/
website/dist/
.astro/
website/.astro/
# Runtime config — DB bootstrap only (secrets live in PostgreSQL).
postgres-data/
data/config.json
data/ttt.db
backups/

*.log
npm-debug.log*
# OS core dumps at repo root only (do not use bare "core" — that would ignore src/core/)
/core
/core.*
.DS_Store
.cursor/
.vscode/
.ssh/

_tmp/
