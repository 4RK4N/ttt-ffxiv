node_modules/
dist/
website/dist/
.astro/
website/.astro/
# Runtime config holds secrets (token, OAuth, channel IDs). Keep the *.example.json
# templates tracked, but never commit the real config.json files.
data/config.json
data/*/config.json
data/*/texts.json

*.log
npm-debug.log*
# OS core dumps at repo root only (do not use bare "core" — that would ignore src/core/)
/core
/core.*
.DS_Store
.cursor/
.vscode/
.ssh/

_tmp/
