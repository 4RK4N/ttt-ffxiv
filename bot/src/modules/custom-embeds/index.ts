import type { CommandModule } from '../../moduleLoader.js';
import { NAMESPACE } from '../../../../shared/modules/custom-embeds/config-io.js';

const customEmbedsModule: CommandModule = {
  name: NAMESPACE,
};

export default customEmbedsModule;
