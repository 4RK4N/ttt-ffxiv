/** Self-hosted Tabler (copied to dist on build). */
export const TABLER_CSS_LOCAL = '/assets/css/tabler.min.css';

/** CDN fallback when dist copy is missing (e.g. stale deploy). Matches @tabler/core in package.json. */
export const TABLER_CSS_CDN =
  'https://cdn.jsdelivr.net/npm/@tabler/core@1.4.0/dist/css/tabler.min.css';

export const OVERRIDES_CSS = '/assets/css/admin-overrides.css';
