---
import type { Lang } from "../data/nav";

interface Props {
  lang: Lang;
  altDe: string;
  altEn: string;
}

const { lang, altDe, altEn } = Astro.props;

const target = lang === "de" ? altEn : altDe;
const label = lang === "de" ? "Zu Englisch wechseln" : "Switch to German";
const title = lang === "de" ? "English" : "Deutsch";
---

<a
  href={target}
  data-astro-prefetch
  aria-label={label}
  title={title}
  class="inline-flex h-9 w-9 shrink-0 items-center justify-center site-control hover:border-bar-accent"
>
  {
    lang === "de" ? (
      <svg
        viewBox="0 0 5 3"
        class="h-3 w-5 rounded-[2px] shadow-sm"
        aria-hidden="true"
      >
        <rect width="5" height="1" fill="#000" />
        <rect y="1" width="5" height="1" fill="#dd0000" />
        <rect y="2" width="5" height="1" fill="#ffce00" />
      </svg>
    ) : (
      <svg
        viewBox="0 0 60 30"
        class="h-3 w-5 rounded-[2px] shadow-sm"
        aria-hidden="true"
      >
        <rect width="60" height="30" fill="#012169" />
        <path d="M0,0 60,30 M60,0 0,30" stroke="#fff" stroke-width="6" />
        <path d="M0,0 60,30 M60,0 0,30" stroke="#c8102e" stroke-width="3" />
        <path d="M30,0 v30 M0,15 h60" stroke="#fff" stroke-width="10" />
        <path d="M30,0 v30 M0,15 h60" stroke="#c8102e" stroke-width="6" />
      </svg>
    )
  }
</a>
